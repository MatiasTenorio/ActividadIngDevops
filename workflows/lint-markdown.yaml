name: WorkFlow 


on:
  push:
    branches:
    - main


jobs:
  actividad:
    runs-on: self-hosted
    steps:
      - name: "Run Markdownlint"  
        uses: davidanson/markdownlint-cli2-action@v16         
        with:  
          config: '.markdownlint.json' 
        globs: '**/*.md' 